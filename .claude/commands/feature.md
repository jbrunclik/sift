Implement a new feature: $ARGUMENTS

Steps:
1. Read `docs/roadmap.md` and `docs/product-spec.md` to understand context and current state.
2. Plan the implementation: identify which files to create/modify, any new migrations, API endpoints, frontend changes.
3. Implement the feature following CLAUDE.md conventions.
4. Run `make lint`, `make typecheck`, `make test`. Fix any issues.
5. Update `docs/roadmap.md` to mark the work as done or add new items.
6. Summarize what was done and what to test manually.
